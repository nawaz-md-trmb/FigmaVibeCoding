// @ts-nocheck
import { LogoMarquee } from "./LogoMarquee";

/** Pattern docs live preview — same component as the blueprint home trusted-by band. */
export function LogoMarqueePattern() {
  return <LogoMarquee />;
}

export default LogoMarqueePattern;
